# GPU training stack (Colab, local CUDA). Install:
#   pip install -r requirements-train.txt
#
# NOTE: Unsloth removed intentionally. It monkey-patches TRL internals
# (sampling_params.split) and crashes on any TRL version change.
# We use the stable HF stack: transformers + bitsandbytes + peft + trl.
datasets>=2.14.0
matplotlib>=3.7.0
numpy>=1.24.0
trl>=0.9.0
transformers>=4.40.0
bitsandbytes>=0.43.0
peft>=0.10.0
accelerate>=0.29.0
