# ---------------------------------------------------------------------------
# Hugging Face Spaces installs only this file (no `-r` includes; the Hub
# build mounts this file alone, so secondary requirement files are not visible).
# Keep it CPU-light: Gradio demo only. For GRPO training use requirements-train.txt
# For the OpenEnv HTTP server use requirements-server.txt
# ---------------------------------------------------------------------------
gradio>=4.44.0
requests>=2.31.0
